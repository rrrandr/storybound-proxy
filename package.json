{
  "name": "storybound-proxy",
  "version": "1.0.0",
  "private": true,

  "description": "Proxy server for Storybound (LLM + image fallbacks)",

  "engines": {
    "node": "18.x"
  },

  "type": "commonjs",

  "scripts": {
    "start": "node api/proxy/index.js"
  },

  "dependencies": {
    "axios": "^1.6.8"
  }
}
